import { styled } from "..";

export const HomeContainer = styled("main", {
  display: "flex",
  // gap: "3rem",
  width: "100%",

  alignItems: "center",
  justifyContent: "center",
  marginLeft: "auto",
  minHeight: 656,
});
