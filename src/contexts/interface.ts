export interface FIPE {
    codigo: string;
    nome: string;
  }
  export interface Query {
   codigoMarca : string; 
   codigoModelo : string;
   codigoAno : string;  
  }
  export interface ResultProps {
    marca: string;
    modelo: string;
    ano: string;
    valor: string
  }

 